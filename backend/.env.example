# Canopy backend environment variables.
# Copy to backend/.env (gitignored) and fill in real values for local dev.
# Production values live in GitHub Secrets.

# Google Solar API key (server-side only)
GOOGLE_SOLAR_KEY=

# Google Gemini API key (server-side only)
GEMINI_API_KEY=
